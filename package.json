{
  "name": "my-nodejs-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && mkdir -p build && cp -r public/* build/",
    "test": "jest --ci --forceExit",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^9.23.0",
    "jest": "^29.5.0",
    "jest-junit": "^16.0.0",
    "supertest": "^6.3.4"
  }
}
